Make a conservative architecture or PPO-hyperparameter improvement for HalfCheetah-v4.

Only change the shown chunk. Do not turn this into image classification, tabular modeling, or a standalone training script.

The current code block:
```python
{}
```
